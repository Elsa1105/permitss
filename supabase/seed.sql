-- Seed: optional demo qualified-personnel rows.
-- Real data is loaded via the Admin > Users CSV upload in-app.

-- (No seed users by default --- create them via Supabase Auth invites,
--  then the on_auth_user_created trigger inserts public.users rows.)
